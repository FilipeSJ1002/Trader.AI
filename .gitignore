# Ignorar arquivos temporários do Python
__pycache__/
*.py[cod]
*$py.class
.env
venv/
.vscode/

# Ignorar Datasets gigantes (GitHub não aceita arquivos > 100MB facilmente)
*.csv
*.parquet
*.zip
data/
datasets_brutos/
data_v*/

# Modelos treinados (binários pesados — não versionar)
*.pth
*.pt
*.ckpt
*.meta.json

# ─────────────────────────────────────────────────────────────────────────
# TODA saída gerada em runtime mora em relatorios/ — logs, relatórios de
# backtest, resultados de experimento. É descartável e 100% regenerável
# (o código que a produz está versionado). Uma pasta, uma regra.
# ─────────────────────────────────────────────────────────────────────────
relatorios/

# Estado operacional do bot (histórico real — não é código, não versionar)
trading_state.db
v5_live_state.json
v5_live_trades.csv
v6_executor_state.json
v5_train.pause

# Documentos de trabalho e relatórios HTML (pesados; distorcem a estatística
# de linguagem do GitHub)
_LEIA_PRIMEIRO.md
requirements_utf8.txt
RELATORIO_PROJETO.txt
pip_temp/
backtest_report.html
*.html
*.log

# Artefatos que o CatBoost gera sozinho ao treinar (nao sao codigo)
catboost_info/
